import { createTheme } from "@mui/material";

const webTheme = createTheme({
    colorSchemes: {
      light: {
        palette: {
          
          // ...other tokens
        },
      },
      dark: {
        palette: {
          
          // ...other tokens
        },
      },
    },
  });

export default webTheme;
